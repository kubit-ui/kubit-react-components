/**
 * @description
 * enum for the avatar content type
 * @enum AvatarContentType
 */
export enum AvatarContentType {
  WITH_INITIALS = 'WITH_INITIALS',
  WITH_ICON = 'WITH_ICON',
  WITH_IMAGE = 'WITH_IMAGE',
}

/**
 * @description
 * enum for the avatar background color
 * @enum AvatarBackgroundColor
 */
export enum AvatarBackgroundColor {
  COLOR_DEFAULT = 'COLOR_DEFAULT',
  COLOR_RED = 'COLOR_RED',
  COLOR_WHITE = 'COLOR_WHITE',
}
