/**
 * Badge status
 * @enum {string}
 * @property {string} OPEN - Badge status open.
 * @property {string} CLOSE - Badge status close.
 */

export enum BadgeStatus {
  OPEN = 'OPEN',
  CLOSE = 'CLOSE',
}
