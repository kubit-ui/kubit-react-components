/**
 * Badge state
 * @enum {string}
 * @property {string} OPEN - Badge state open.
 * @property {string} CLOSE - Badge state close.
 */

export enum BadgeState {
  ACTIVE = 'ACTIVE',
  DEFAULT = 'DEFAULT',
}
