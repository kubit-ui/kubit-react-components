import { SIZES } from '@/types';

export enum IconHighlightedSizeType {
  EXTRA_SMALL = SIZES.EXTRA_SMALL,
  SMALL = SIZES.SMALL,
  MEDIUM = SIZES.MEDIUM,
  LARGE = SIZES.LARGE,
  EXTRA_LARGE = SIZES.EXTRA_LARGE,
}
