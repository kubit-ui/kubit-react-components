import { SIZES } from '@/types';

/**
 * @name MediaButtonSizeType
 * @description
 * MediaButton size type
 */
export enum MediaButtonSizeType {
  LARGE = SIZES.LARGE,
  SMALL = SIZES.SMALL,
  EXTRA_LARGE = SIZES.EXTRA_LARGE,
}
