export enum PageControlState {
  CURRENT = 'CURRENT',
  DEFAULT = 'DEFAULT',
  LAST = 'LAST',
}

export enum ArrowsControlState {
  INACTIVE = 'INACTIVE',
  ACTIVE = 'ACTIVE',
}

export enum PageControlDirectionType {
  FORTH = 'FORTH',
  BACK = 'BACK',
}
