/**
 * When format list format, allows to select the list direction (from the table)
 */
export enum FormatListHeaderPriorityType {
  // Should be deprecated, it does not appears in the new designs ?
  ROW = 'ROW',
  ROW_HEADER = 'ROW_HEADER',
  COLUMN_HEADER = 'COLUMN_HEADER',
}
