export enum TableVariantType {
  DEFAULT = 'DEFAULT',
}

export enum TableHeaderVariantType {
  PRIMARY = 'PRIMARY',
  SECONDARY = 'SECONDARY',
  CUSTOMIZABLE_HEADER = 'CUSTOMIZABLE_HEADER',
}

export enum TableRowVariantType {
  DEFAULT = 'DEFAULT',
  CUSTOMIZABLE_ROW = 'CUSTOMIZABLE_ROW',
}
