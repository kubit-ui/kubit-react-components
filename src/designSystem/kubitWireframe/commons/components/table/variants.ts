export enum TableVariantType {
  DEFAULT = 'DEFAULT',
  DEFAULT_WITH_FOOTER = 'DEFAULT_WITH_FOOTER',
}

export enum TableHeaderVariantType {
  DEFAULT = 'DEFAULT',
}

export enum TableRowVariantType {
  DEFAULT = 'DEFAULT',
}
